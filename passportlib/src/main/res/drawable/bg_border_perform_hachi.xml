<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 背景色 -->
    <solid android:color="@android:color/transparent" />
    <stroke android:color="@color/red_hachi" android:width="@dimen/border_width_normal" />
    <!-- 边角：设置圆角大小 -->
    <corners android:radius="@dimen/radius_size_small" />
</shape>